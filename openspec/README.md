# AIES OpenSpec Root

This is the canonical OpenSpec root for AIES v2.

Under `openspec\changes\`, the canonical v1 shape is one Markdown file per change using YAML frontmatter and short sections for summary, current task, pending tasks, blockers, and notes.
