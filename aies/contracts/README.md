# AIES Contracts

This directory defines the stable internal contracts for AIES v2 modules.

Phase 0 rules:
- contracts are serialization-friendly
- contracts contain no runtime policy logic
- shared names should remain stable across later slices
- AIES modules should exchange data through these contracts instead of implicit side effects

